# Trained Models

This folder stores serialized machine learning artifacts.

Included

- StandardScaler

The trained Random Forest model is intentionally excluded because its size exceeds GitHub's upload limit.

The model can be regenerated by running the notebook.
